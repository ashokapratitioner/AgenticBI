# ─────────────────────────────────────────────
# Agentic BI POC — Environment Configuration
# ─────────────────────────────────────────────

# Google Cloud Project
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1

# BigQuery Dataset
BQ_DATASET=erp_poc

# Google Application Credentials (path to service account JSON)
GOOGLE_APPLICATION_CREDENTIALS=

# Demo Mode — set to "true" to run without GCP (uses mock data)
DEMO_MODE=true

# ── Email Agent (Gmail SMTP) ──────────────────
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=testpranshu900@gmail.com
SMTP_PASSWORD=
# Note: For Gmail, use an App Password (not your login password)
# Generate at: https://myaccount.google.com/apppasswords

# Default email recipients (comma-separated)
DEFAULT_RECIPIENTS=
